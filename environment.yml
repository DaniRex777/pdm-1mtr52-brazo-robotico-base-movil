# Entorno virtual (conda) del servidor central
# Crear con : conda env create -f environment.yml
# Activar   : conda activate lab-iot-teleop
name: lab-iot-teleop
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi==0.115.6
      - uvicorn[standard]==0.34.0
      - paho-mqtt==1.6.1
      - asyncpg==0.30.0
      - xArm-Python-SDK==1.14.7
